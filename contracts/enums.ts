export enum TaskPriority {
    HIGH = 'high',
    MEDIUM = 'medium',
    LOW = 'low',
}

export enum RequestType {
    FRIEND = 'friend',
    CHALLENGE = 'challenge',
}

export enum Status {
    ACCEPT = 'accept',
    PENDING = 'pending',
    REJECT = 'reject',
}
